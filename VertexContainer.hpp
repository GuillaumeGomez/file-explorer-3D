#ifndef VERTEXCONTAINER_HPP
#define VERTEXCONTAINER_HPP

#include <map>
#include <vector>
#include "GL/glew.h"

/*class VertexContainer
{
public:
  static VertexContainer  *getInstance();
  void                    addVertices(GLuint drawCommand, std::vector<GLfloat>);

private:
  VertexContainer();
  virtual ~VertexContainer();
  static VertexContainer  *m_instance;

  std::map<GLuint, std::vector<GLfloat> >   m_container;
};*/

#endif // VERTEXCONTAINER_HPP
